```c
#include <stdio.h>
int main()
{
    int num ,i, tab;
    printf("enter the number: ");
    scanf(%d" , &num);
        printf("/ntable of %d is :/n" ,num);
        for(i=1; i<+10; i+=)
        {
        tab = num*i;
        printf("%d *%d =%d/n" , num,i,tab);
        }
        getch();
        return 0;
    }